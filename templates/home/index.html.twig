{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="page-wrapper">
        <div class="page-content container">
            <div class="container">
                <div class="content-wrapper">
                    <div class="landing-page-title">Scriber</div>
                    <h1 class="landing-page-content">Jei transkribavimas varo į neviltį ir spaudžia terminai, tuomet <em>Scriber</em> - automatinio audio ir video transkribavimo įrankis yra skirtas kaip tik Jums!</h1>
                </div>
                {% if not app.user %}
                <div class="content-wrapper">
                    <h2>
                        <a class="button landing-page-content" href="{{ path('register') }}">Išbandykite nemokamai</a>
                    </h2>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}